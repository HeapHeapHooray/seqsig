[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "seqsig"
version = "0.1.0"
description = "SeqSig: 512-Bit Secret Seed Sequential Signature & Identity CLI"
readme = "README.md"
authors = [
    { name = "SeqSig Developers" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
seqsig = "seqsig.cli:main"
